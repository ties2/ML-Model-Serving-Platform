# ==========================================
# 1. API & Serving
# ==========================================
fastapi>=0.100.0     # Main framework for building APIs
uvicorn==0.29.0      # Server for running the application
pydantic>=2.0.0      # Data validation
pydantic-settings>=2.0.0
pytest>=7.0.0
httpx>=0.25.0
requests>=2.31.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
# ==========================================
# 2. Machine Learning & Data Processing
# ==========================================
# pandas>=2.0.0      # (Future) Data processing and management
scikit-learn>=1.3.0 # (Future) Building and training base models
# numpy>=1.24.0      # (Future) Numerical computing
joblib>=1.3.2

# ==========================================
# 3. MLOps & Tracking
# ==========================================
# mlflow>=2.6.0      # (Future) Experiment tracking and model versioning
# dvc>=3.0.0         # (Future) Data Version Control
prometheus-client>=0.19.0
# ==========================================
# 4. Development, Linting & Testing
# ==========================================
ruff>=0.3.0        # (Future) Fast Python linting and formatting
pytest>=6.0.0      # (Future) Running automated tests
pre-commit>=3.3.0  # (Future) Running hooks automatically before committing code
PyYAML>=6.0